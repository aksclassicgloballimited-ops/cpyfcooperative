'use client';

import { useEffect, useState } from 'react';
import Link from 'next/link';

type Member = { id: string; firstName: string; lastName: string; email: string; phone: string; role: string; membership?: { membershipNo: string; status: string; grade: string; category: string } };
const roles = ['MEMBER', 'SUPER_ADMIN', 'FINANCE_OFFICER', 'LOAN_OFFICER', 'MEMBERSHIP_OFFICER', 'AUDITOR', 'EXECUTIVE', 'ADMIN'];
export default function AdminMembersPage() {
  const [members, setMembers] = useState<Member[]>([]);
  const [search, setSearch] = useState('');
  const [status, setStatus] = useState('');
  const [message, setMessage] = useState('');
  const load = () => fetch(`/api/members?search=${encodeURIComponent(search)}${status ? `&status=${status}` : ''}`, { cache: 'no-store' }).then((r) => r.json()).then((data) => setMembers(data.members ?? [])).catch(() => setMessage('Unable to load members'));
  useEffect(() => { load(); }, []);
  const update = async (userId: string, data: Record<string, string>) => { const response = await fetch('/api/members', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ userId, ...data, reason: 'Admin member management update' }) }); const result = await response.json(); setMessage(response.ok ? 'Member updated.' : result.error || 'Update failed'); if (response.ok) load(); };
  return <main className="min-h-screen bg-[#f8f5ff] px-4 py-10 text-[#1d1731] sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl"><Link href="/admin" className="font-bold text-[#6A11CB]">← Back to admin</Link><div className="mt-5 rounded-[2rem] bg-[#1d1234] p-7 text-white"><p className="text-xs font-bold uppercase tracking-[0.2em] text-violet-200">General Secretary</p><h1 className="mt-2 text-3xl font-bold">Member Management</h1></div><section className="mt-6 rounded-[2rem] border border-violet-200 bg-white p-6 shadow-sm"><div className="flex flex-wrap gap-3"><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search name, email, phone or membership number" className="min-w-[280px] flex-1 rounded-xl border border-violet-200 px-3 py-2" /><select value={status} onChange={(e) => setStatus(e.target.value)} className="rounded-xl border border-violet-200 px-3 py-2"><option value="">All statuses</option><option value="PENDING">Pending</option><option value="ACTIVE">Active</option><option value="SUSPENDED">Suspended</option><option value="REJECTED">Rejected</option></select><button type="button" onClick={load} className="rounded-full bg-[#6A11CB] px-5 py-2 font-bold text-white">Search</button></div>{message && <p className="mt-4 text-sm font-semibold text-[#4C1D95]">{message}</p>}<div className="mt-5 overflow-x-auto rounded-xl border border-violet-100"><table className="w-full min-w-[980px] text-left text-sm"><thead className="bg-violet-50 text-[#4C1D95]"><tr><th className="px-4 py-3">Member</th><th className="px-4 py-3">Membership</th><th className="px-4 py-3">Category</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Role</th><th className="px-4 py-3">Actions</th></tr></thead><tbody>{members.map((member) => <tr key={member.id} className="border-t border-violet-100"><td className="px-4 py-3"><b>{member.firstName} {member.lastName}</b><span className="block text-xs text-slate-500">{member.email}</span></td><td className="px-4 py-3 font-semibold">{member.membership?.membershipNo}</td><td className="px-4 py-3">{member.membership?.grade}<span className="block text-xs text-slate-500">{member.membership?.category}</span></td><td className="px-4 py-3">{member.membership?.status}</td><td className="px-4 py-3"><select value={member.role} onChange={(e) => update(member.id, { role: e.target.value })} className="rounded-lg border border-violet-200 px-2 py-1">{roles.map((role) => <option key={role}>{role}</option>)}</select></td><td className="px-4 py-3"><div className="flex flex-wrap gap-2">{member.membership?.status === 'PENDING' && <button type="button" onClick={() => update(member.id, { status: 'ACTIVE' })} className="rounded-full bg-[#6A11CB] px-3 py-1 text-xs font-bold text-white">Approve</button>}{member.membership?.status === 'ACTIVE' && <button type="button" onClick={() => update(member.id, { status: 'SUSPENDED' })} className="rounded-full border border-amber-200 px-3 py-1 text-xs font-bold text-amber-700">Suspend</button>}{member.membership?.status === 'SUSPENDED' && <button type="button" onClick={() => update(member.id, { status: 'ACTIVE' })} className="rounded-full border border-emerald-200 px-3 py-1 text-xs font-bold text-emerald-700">Activate</button>}<Link href={`/member/transactions?userId=${member.id}`} className="rounded-full border border-violet-200 px-3 py-1 text-xs font-bold text-[#6A11CB]">Records</Link></div></td></tr>)}</tbody></table></div></section></div></main>;
}
